# AI

### how to run this program

```sh
python3 -m pip install pip
pip install -r requirements.txt
```

### start jupyter

cd into project root
build on firt run

```sh
jupyter lab build
```

```sh
jupyter lab .
```
